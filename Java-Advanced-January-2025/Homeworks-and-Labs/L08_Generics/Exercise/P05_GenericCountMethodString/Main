package L08_Generics_Exercises.P05_GenericCountMethodString;

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        short n = Short.parseShort(scanner.nextLine());

        Box<String> box = new Box<>();
        for (int i = 1; i <= n ; i++) {
            String input = scanner.nextLine();
            box.add(input);
        }

        String elementToCompare = scanner.nextLine();
        System.out.println(box.countGreaterElements(elementToCompare));


    }
}

